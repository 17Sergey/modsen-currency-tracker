import styled from "styled-components";

import { device } from "@styles/breakpoints";

export const StyledImg = styled.img`
    width: 2.5rem;
    height: 2.5rem;
`;

export const StyledDiv = styled.img`
    /* @media only screen and (${device.sm}) {
        display: block;
    } */
`;
